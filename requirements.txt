# ============================================================================
# LLMUI Core — Dépendances Python
# Copyright © Technologies Nexios TF Inc. — nexiostf.com
# ============================================================================
# Liste limitée aux dépendances réellement importées par le code
# (relevé AST de src/, scripts/create_admin.py, tools/test_login.py).
# Aucune pile ML lourde (torch/transformers) : non utilisée par LLMUI Core,
# qui délègue toute l'inférence à Ollama (STANDARDS.md §7, §15).
# ============================================================================

# WEB / API (FastAPI — STANDARDS.md §3)
fastapi==0.109.2
uvicorn[standard]==0.24.0
starlette==0.36.3
python-multipart==0.0.7
itsdangerous==2.1.2          # Signature des cookies de session (SessionMiddleware)

# HTTP — appels vers Ollama local
httpx==0.25.2

# VALIDATION
pydantic==2.8.2

# DATE / FUSEAU HORAIRE
pytz==2024.1

# BASE DE DONNÉES — PostgreSQL async + migrations (STANDARDS.md §2 — jamais SQLite)
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36
alembic==1.12.1

# CACHE / RATE LIMITING (STANDARDS.md — Redis)
redis==5.0.1

# SÉCURITÉ (STANDARDS.md §6 — Argon2 obligatoire, jamais bcrypt/SHA/MD5)
argon2-cffi==23.1.0
passlib[argon2]==1.7.4
pyotp==2.9.0                 # TOTP (RFC 6238)
cryptography==42.0.4         # Fernet — chiffrement des secrets TOTP au repos

# OUTILLAGE & TESTS (STANDARDS.md §10 — ruff ; §11 — pytest)
ruff==0.6.9
pytest==7.4.3
pytest-asyncio==0.21.1
