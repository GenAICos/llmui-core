# Copyright © Technologies Nexios TF Inc. — nexiostf.com
# Généré par l'installateur FinanceFamille

[Unit]
Description=FinanceFamille - Budget Familial — Technologies Nexios TF Inc.
After=network.target postgresql.service

[Service]
Type=exec
User={{ app_user }}
Group={{ app_user }}
WorkingDirectory={{ install_dir }}
ExecStart={{ install_dir }}/venv/bin/uvicorn app.main:app \
          --host 127.0.0.1 --port {{ app_port }} --workers 2
Restart=always
RestartSec=5
StandardOutput=append:{{ install_dir }}/logs/financefamille.log
StandardError=append:{{ install_dir }}/logs/financefamille.log
NoNewPrivileges=yes
PrivateTmp=yes
ProtectSystem=strict
ReadWritePaths={{ install_dir }}/data {{ install_dir }}/logs {{ install_dir }}/exports

[Install]
WantedBy=multi-user.target
